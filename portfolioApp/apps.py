from django.apps import AppConfig

class PortfolioappConfig(AppConfig):
    name = 'portfolioApp'

    # def ready(self):
        # from portfolioApp.views import load_instrument_list
        # load_instrument_list()